# Bundled maps

Upload `.sspm` files directly into this folder and commit to main. GitHub Actions copies them into the game, and they appear in every browser profile after deployment. Use SSPM v1 or v2 maps with embedded audio. Files must be below 100 MiB for GitHub. Only upload maps and music you can redistribute.
